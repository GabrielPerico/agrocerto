{
  "private": true,
  "name": "@acme/api",
  "types": "./src/index.ts",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
